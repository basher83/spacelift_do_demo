# ansible/inventory/inventory.yml
# This is a placeholder inventory file for Ansible planning phase
# The actual inventory will be generated by the Spacelift run hook
all:
  children:
    webservers:
      hosts:
        localhost:
          ansible_connection: local
          ansible_python_interpreter: /usr/bin/python3
          ansible_become: false