# ansible/roles/app/templates/docker-compose.yml.j2
services:
  app:
    image: lscr.io/linuxserver/nginx:1.26.3
    container_name: webapp
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./html:/usr/share/nginx/html